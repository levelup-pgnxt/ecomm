version: '3.9'

services:

  mongo:
    image: mongo
    container_name: mongo-ecomm
    restart: always
    post:
     - 27017:27017
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: secret

      